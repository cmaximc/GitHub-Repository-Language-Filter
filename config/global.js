export const config = {
    // GitHub personal access token
    // Keep in mind there is a rate limit for requests
    authToken: '',
    organization: 'RoyalAholdDelhaize',
    languagesToCheck: ['Java', 'PLSQL']
};